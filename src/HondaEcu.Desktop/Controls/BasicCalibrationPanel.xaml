<UserControl x:Class="HondaEcu.Desktop.Controls.BasicCalibrationPanel"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="clr-namespace:HondaEcu.Desktop.Controls">
 <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="14">
  <StackPanel>
   <TextBlock Text="Базові налаштування · M1t" FontSize="22" FontWeight="SemiBold" />
   <TextBlock Text="{Binding Prerequisites}" TextWrapping="Wrap" Margin="0,8" />
   <WrapPanel>
    <Button Content="Обрати reviewed definition" Command="{Binding SelectLocationCommand}" />
    <Button Content="Відкрити M1t результат" Command="{Binding OpenChildCommand}" />
    <Button Content="Взяти налаштування з плану" Command="{Binding ReuseCommand}" />
   </WrapPanel>
   <TextBlock Text="Original, profile та binding обираються спільними кнопками вгорі. Новий draft не включає старий pending VTEC edit." TextWrapping="Wrap" Margin="0,8" />
   <StackPanel IsEnabled="{Binding CanEdit}">
    <TextBlock Text="VTEC · один threshold code, raw" FontSize="18" FontWeight="SemiBold" Margin="0,12,0,4" />
    <ComboBox x:Name="VtecSlot" ItemsSource="{Binding Draft.Slots}" DisplayMemberPath="Id" SelectedValuePath="Id" SelectedValue="{Binding Draft.SelectedSlot, Mode=TwoWay}" />
    <TextBlock Text="{Binding Draft.SlotExplanation}" TextWrapping="Wrap" Margin="0,4" />
    <TextBlock Text="Відсічка: fixed — raw period pair; bank0/bank1 — незалежні bases для producer поточних RAM-порогів." TextWrapping="Wrap" FontWeight="SemiBold" Margin="0,8" />
    <TextBlock Text="Холостий хід: base target може бути замінений override або late lookup. DATA027A не додається до target у перевіреній межі. Axes — лише для читання." TextWrapping="Wrap" Margin="0,4" />
    <ItemsControl x:Name="Groups" ItemsSource="{Binding Draft.Groups}">
     <ItemsControl.ItemTemplate><DataTemplate>
      <Border BorderBrush="#D9E2EC" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,6">
       <StackPanel>
        <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" />
        <WrapPanel>
         <CheckBox Content="Включити зміну" IsChecked="{Binding Included, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,16,0" />
         <Button Content="Скинути групу до original" Command="{Binding ResetCommand}" />
        </WrapPanel>
        <DataGrid ItemsSource="{Binding Fields}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False"
         HeadersVisibility="Column" RowHeaderWidth="0" HorizontalScrollBarVisibility="Auto" IsEnabled="{Binding Included}" MinHeight="64" MaxHeight="300">
         <DataGrid.Columns>
          <DataGridTextColumn Header="Поле / cell" Binding="{Binding Id}" IsReadOnly="True" Width="*" MinWidth="160" />
          <DataGridTextColumn Header="Raw axis" Binding="{Binding Axis}" IsReadOnly="True" Width="95" />
          <DataGridTextColumn Header="Original raw" Binding="{Binding Original}" IsReadOnly="True" Width="115" />
          <DataGridTemplateColumn Header="Requested raw" Width="140">
           <DataGridTemplateColumn.CellTemplate><DataTemplate>
            <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=LostFocus, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" />
           </DataTemplate></DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
         </DataGrid.Columns>
        </DataGrid>
       </StackPanel>
      </Border>
     </DataTemplate></ItemsControl.ItemTemplate>
    </ItemsControl>
   </StackPanel>
   <WrapPanel Margin="0,10">
    <Button Content="Імпорт settings JSON" Command="{Binding ImportCommand}" />
    <Button Content="Зберегти settings JSON" Command="{Binding ExportSettingsCommand}" />
    <Button Content="Скинути весь draft" Command="{Binding ResetCommand}" />
    <Button x:Name="PreviewButton" Content="Переглянути зміни" Command="{Binding PreviewCommand}" />
   </WrapPanel>
   <TextBlock Text="{Binding Error}" Foreground="#A51F28" TextWrapping="Wrap" FontWeight="SemiBold" />
   <TextBlock Text="{Binding PreviewText}" TextWrapping="Wrap" Margin="0,8" />
   <DataGrid x:Name="DiffTable" ItemsSource="{Binding Diff}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column" MaxHeight="300" HorizontalScrollBarVisibility="Auto">
    <DataGrid.Columns>
     <DataGridTextColumn Header="Offset" Binding="{Binding Offset}" Width="100" />
     <DataGridTextColumn Header="Old byte" Binding="{Binding Old}" Width="95" />
     <DataGridTextColumn Header="New byte" Binding="{Binding New}" Width="95" />
     <DataGridTextColumn Header="Призначення" Binding="{Binding Purpose}" Width="*" MinWidth="160" />
    </DataGrid.Columns>
   </DataGrid>
   <TextBlock Text="Модель порогового порівняння VTEC — не фізичне перемикання" TextWrapping="Wrap" Margin="0,12,0,0" />
   <local:PredicatePlot Rows="{Binding VtecPlot}" Height="220" />
   <ItemsControl ItemsSource="{Binding IdlePlots}">
    <ItemsControl.ItemTemplate><DataTemplate><StackPanel>
     <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
     <TextBlock Text="Lookup-модель, не фактичні RPM. X: rawD9; Y: raw target period. Синій: original; помаранчевий: requested. Вузли позначені колами." TextWrapping="Wrap" />
     <local:BasicLookupPlotControl Plot="{Binding}" Height="240" />
    </StackPanel></DataTemplate></ItemsControl.ItemTemplate>
   </ItemsControl>
   <WrapPanel Margin="0,12"><Button x:Name="SaveButton" Content="Зберегти тільки базові налаштування — PC-only" Command="{Binding SaveCommand}" /></WrapPanel>
   <ProgressBar IsIndeterminate="{Binding IsBusy}" Height="5" />
   <TextBlock Text="{Binding ProgressText}" TextWrapping="Wrap" Margin="0,8" />
   <TextBlock Text="{Binding ResultText}" TextWrapping="Wrap" />
   <DataGrid ItemsSource="{Binding Evidence}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column" Margin="0,8">
    <DataGrid.Columns>
     <DataGridTextColumn Header="Evidence section" Binding="{Binding Suite}" Width="*" MinWidth="160" />
     <DataGridTextColumn Header="Strict" Binding="{Binding Completed}" Width="80" />
     <DataGridTextColumn Header="Required" Binding="{Binding Required}" Width="90" />
     <DataGridTextColumn Header="Одиниці" Binding="{Binding Unit}" Width="*" MinWidth="150" />
    </DataGrid.Columns>
   </DataGrid>
   <ItemsControl ItemsSource="{Binding Witnesses}"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding}" TextWrapping="Wrap" /></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
   <TextBlock Text="Незалежні локальні software suites, не один спільний ECU main loop. VtecFullChain/P1, joint ECU execution, hardware/full boot: NotRun. M1h conditional RPM залишається окремим." TextWrapping="Wrap" Margin="0,12" />
  </StackPanel>
 </ScrollViewer>
</UserControl>
